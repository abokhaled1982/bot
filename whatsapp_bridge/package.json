{
  "name": "whatsapp-bridge",
  "version": "0.1.0",
  "description": "WhatsApp Web bridge for the copy-trader bot. Outbound via HTTP, inbound via webhook.",
  "private": true,
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.24.0"
  }
}
